### Enum for user roles (RBAC)
enum: UserRole
serialized: byName   # Use name for serialization (recommended)
values:
  - anant           # Super Admin (Company Level)
  - admin           
  - teacher
  - student
  - accountant
  - parent
  - principal
  - clerk
  - librarian
  - transport
  - hostel