# This is the configuration file for your Development environment (using Staging mode).
# Connects to the Development Database (e.g. Neon Dev Branch).

# Configuration for the main API server.
apiServer:
  port: 8080
  publicHost: anant-server-dev-PLACEHOLDER.a.run.app
  publicPort: 443
  publicScheme: https

# Configuration for the Insights server.
insightsServer:
  port: 8081
  publicHost: anant-server-dev-PLACEHOLDER.a.run.app
  publicPort: 443
  publicScheme: https

# Configuration for the web server.
webServer:
  port: 8082
  publicHost: anant-server-dev-PLACEHOLDER.a.run.app
  publicPort: 443
  publicScheme: https

# Development Database Configuration (Neon)
# TODO: Update these values with your Development Database details
database:
  host: ep-ancient-bird-ahe553x9-pooler.c-3.us-east-1.aws.neon.tech
  port: 5432
  name: neondb
  user: neondb_owner
  requireSsl: true

# Redis configuration (disabled)
redis:
  enabled: false
  host: localhost
  port: 6379
